package com.fhdo.parkingservice.model;

/**
 * Enum representing sorting preferences for nearby parking responses.
 */
public enum SortingPreference {
    LEAST_WALKING_DISTANCE,
    LEAST_TRIP_TIME
}
